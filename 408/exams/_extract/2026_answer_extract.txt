2026 年全国硕士研究生入学统一考试计算机（408）答案及解析
第1 题：A
解析：顺序存储结构中，表头插入、表头删除都需要移动后方全部元素；表尾插
入、表尾删除无需移动元素，因此Ⅰ、Ⅱ会产生移动次数。
第2 题：D
解析：遍历每个结点cu，若cu->p1 不为空，则cu->p2 赋值为cu->p1->p1
（直接后继）；若cu->p1 为空，说明无后继，cu->p2 置NULL，再移动cu，
只有D 选项完整覆盖两种情况。
第3 题：C
解析：层次遍历根为a；中序(b,e,d,f,c) 为左子树，(g) 为右子树。左子树
根c，c 的左子树(b,e,d,f)；b 无孩子，d 的左e、右f。后序遍历顺序：e f
d c b g a。
第4 题：B
解析：森林转二叉树总节点2+3+4+5+7=21，完全二叉树高度最小，2^5-1=31≥21，
理论最小高度5，但森林转二叉树结构限制，最优构造高度为6。
第5 题：D
解析：构建哈夫曼树，权值1、2 合并为3；3、4 合并7；5、8 合并13；10、
12 合并22；7、13 合并20；20、22 合并根。e 权8，同层为f (10)、g (12)。
第6 题：D
解析：邻接表求顶点入度，需要遍历全部边表O (|E|)，同时存储顶点数组O
(|V|)，时间复杂度max (|V|,|E|)。
第7 题：B

解析：无环有向图最长路径长度不超过n-1，不存在长度等于n 的串，B 错误；
无环路径有限，有环可循环生成更长串，存在小于2n 的串。
第8 题：D
解析：高度4 平衡二叉树，左右子树最大节点差推导得差值为5。
第9 题：B
解析：直接插入排序比较次数与序列有序程度正相关，B 选项序列整体有序度最
高，比较次数最少。
第10 题：A
解析：多关键字分段排序、按主次关键字依次排序符合基数排序的核心特征，快
速、希尔、选择排序不支持多关键字分段排序规则。
第11 题：C
解析：增大k 路归并的k 值，归并趟数d 减少；可用内存大小决定初始归并
段长度；归并趟数受初始归并段数量影响，Ⅱ错误，Ⅰ、Ⅲ正确。
第12 题：B
解析：硬件与软件的接口是操作系统，不是指令集体系结构ISA；A、C、D 描述
均正确。
第13 题：A
解析：10100110B 为负数补码，算术右移3 位得11110100，再算术左移2 位
得11010000B。
第14 题：B
解析：12.1 转IEEE754 单精度浮点数，就近舍入偶数规则下机器数为
414199AAH。

第15 题：A
解析：8 片64M×8 位交叉编址，低3 位片选，地址0018001DH 低3 位为01D，
A 选项地址末尾01D，同芯片。
第16 题：D
解析：超标量流水线属于CPU 微架构硬件实现，不由ISA 指令集体系规定；IO
指令、向量中断、虚拟存储管理均为ISA 定义内容。
第17 题：B
解析：条件跳转、过程返回指令跳转失败时会顺序执行下一条指令；调用、陷阱
指令一定会跳转，不会顺序执行原下一条指令。
第18 题：C
解析：块大小32B，4 路组相联，1024 行，组数= 1024/4=256；地址1028，
块内偏移5 位，块号= 1028/32=32，组号32。
第19 题：A
解析：页号22，16 位虚拟地址页大小256B，页内偏移8 位，页号高8 位，
22 二进制00010110，TLB4 路组相联，组号= 页号%4=2；Tag = 页号/ 4=5，
页表P=0 未装入，TLB 有效位V 不能为1，A 不可能出现。
第20 题：A
解析：单周期每条指令1 周期，CPI=1；多周期每条指令多个周期，CPI>1；流
水线存在冲突、停顿，平均CPI 大于1，Ⅲ错误。
第21 题：D
解析：系统调用指令属于用户态可触发指令，非特权指令；IO、关中断、中断返
回均为仅内核态可执行的特权指令。

第22 题：C
解析：中断响应时保存断点（PC、PSW）由硬件自动完成；开中断、保存通用寄
存器由软件中断服务程序实现。
第23 题：C
解析：装入程序需要修改内存地址、页表，运行在内核态；编译、链接、命令解
释程序运行于用户态。
第24 题：D
解析：地址转换由MMU 硬件完成；页表项由操作系统内核填写；内存缺页等异
常由操作系统处理，C、D 表述一致，选D。
第25 题：D
解析：同一进程所有线程共享堆、全局变量；用户级线程不由OS 创建；用户级
线程映射多个内核线程不符合模型；每个线程独立栈。
第26 题：A
解析：信号量S 初值5，wait (S) 执行S 减1，S=-2 时，成功访问资源进程
m=5，阻塞进程n=2。
第27 题：C
解析：读写冲突判定条件：读集与对方写集无交集、双方写集无交集，即R (Q)∩W
(P)=∅、W (P)∩W (Q)=∅、R (P)∩W (Q)=∅，对应Ⅰ、Ⅲ、Ⅳ。
第28 题：C
解析：三级页表9 位，三级页表项数量2^9=512；每个页框存放512 个页表项，
三级页表占用页框数2^9=512=256K。
第29 题：C

解析：TLB 加速虚实地址转换、工作集减少缺页、页表缓冲队列降低页表访问开
销；多级页表不降低平均访存时间，Ⅱ错误。
第30 题：B
解析：两进程共享同一文件，文件映射物理地址唯一，W1、W2 虚拟地址不同但
物理地址一致；修改互相可见，页表项存储相同物理页框号。
第31 题：D
解析：字符设备按字符流式IO，块设备按块缓存IO，二者IO 方式不同；A、B、
C 描述驱动程序内容正确。
第32 题：D
解析：中断处理程序仅完成将硬件寄存器数据搬运至内核缓冲区；解析指令、同
步用户缓冲区由上层应用程序完成；硬件传输数据到寄存器。
第33 题：B
解析：分层越多，每层封装、头部开销越大，传输与处理效率越低；A、C、D 是
分层体系优势。
第34 题：D
解析：香农公式C=Wlog2 (1+S/N)=200k×10=2Mbps；分组1500B=12000bit，时
延= 12000/2000000=6ms。
第35 题：B
解析：1500B 数据帧传输时长忽略，仅需SIFS 间隔28μs，加上确认帧基础时
长，总最小时间68μs。
第36 题：B

解析：H1、H3 同VLAN，H2、H4、H5 同VLAN，H6 跨VLAN；F1 单播同VLAN 可
达H3，F3 广播帧全网转发，H3 接收；F2、F4 跨VLAN 无法到达H3。
第37 题：C
解析：R1-R2 链路断开后，重新计算路由并聚合，最终路由表共5 条独立路由
项。
第38 题：A
解析：OSPF 支持划分区域；RIP 无区域概念；BGP 是自治系统间外部网关协议，
不属于内部网关。
第39 题：C
解析：123.4.4.0/22 划分32 个子网，子网掩码/ 27，块大小32；123.4.5.11
落在子网123.4.5.192/27。
第40 题：D
解析：Cookie 用于用户跟踪、个性化推荐、购物车存储；缩短Web 响应时间由
缓存、CDN 实现，不属于Cookie 用途。
第41 题解析：
（1）
①利用BST 的中序遍历有序性：
二叉搜索树的中序遍历结果是一个严格递增的序列（假设无重复值）。
因此，最小差值一定出现在相邻的两个节点值之间。
②中序遍历收集节点值：
通过递归中序遍历（左→根→右），将所有节点的值按顺序存入数
组values。
③线性扫描找最小差：
遍历已排序的values 数组，计算每一对相邻元素的差值。

记录并返回最小的差值。
（2）
void inorderTraverse(TreeNode* root, int* values, int* index) {
if (root == NULL) return;
inorderTraverse(root->left, values, index);
values[(*index)++] = root->val;
inorderTraverse(root->right, values, index);
}
int getMinimumDifference(TreeNode* root) {
int values[10000];
int index = 0;
// 中序遍历收集所有值
inorderTraverse(root, values, &index);
// 计算相邻元素最小差值
int minDiff = INT_MAX;
for (int i = 1; i < index; i++) {
int diff = values[i] - values[i-1];
if (diff < minDiff)
minDiff = diff;
}
return minDiff;
}
第42 题解析：
(1) 不能，该序列违反栈出栈规则，存在无法实现的逆序大小关系。
（2)无法由栈生成的三元组满足Pi>Pj>Pk (i<j<k)。
(3) n=4 以2 开头合法出栈序列共5 个：2143、2134、2341、2314、2431。
(4) n=k 时，以1 开头的出栈序列数量等于n=k-1 时总序列数M；以2 开头
数量同样为M；全部出栈序列有
个。

第43 题解析：
(1) 按字节编址，主存单元8 位；机器字长16 位，通用寄存器16 位。
(2) OP1、OP2 编码可相同，分属不同指令类型段；OP2、OP3 分属不同指令编码
域，不可相同。
(3) R2=ABCDH+F001H=9BCEH，R9 不改变，值为F001H。
(4)①1110；②0000 0100；③1111 1011；④0000 0000 0010。
第44 题解析：
(1) 多路选择器(MUX)。
(2) Extop 至少1 位，1 位即可区分零扩展、符号扩展两种模式。
(3) MARSrc=0，ALUSrc=0，ALUSelect=1，RegWr=0。
(4) ALUSelect=2，RegSrc=1，RegDest=1，RegWr=1；不可以相同，左移移位量
需零扩展，M 型offset 地址偏移需要符号扩展，扩展逻辑不同。
第45 题解析：
（1）10ms 的时候，产生第1 次时间中断，P1 和P2 都在就绪队列中，
此时P2 的优先权最高，所以调度P2；
20ms 的时候，发生第2 次时间中断，P4 的优先权最高，所以调度P4；
90ms 的时候，发生第9 次时间中断，P4 运行结束，调度P1；
140ms：发生14 次时间中断，P1 优先权值减1，变为2，回到就绪队列；
此时P3 先进入就绪队列，所以发生22 次时间中断，7 次调度；
(2) 时间片增大，进程切换次数减少，CPU 调度次数减小；中断间隔缩短，中断
触发频率升高，系统开销增大。
第46 题解析：
（1）盘块大小4KB，索引节点占128B，所以一个盘块中可以存放4K/128B
= 32 个索引节点；1000/32 = 31.25，且从100 盘块开始存放，所以file 的

索引节点存放的盘块号是131；直接地址项所表示的文件大小是5×4K=20480B。
21460 放在一组间接地址中，所以最多需要读2 个盘块。(4096×4K)/128B = 2¹⁷
个文件
（2）由于dir1 非空，所以删除dir1 时，需要先删除目录文件file，所以
①释放file 的索引节点和占用的盘块，并且其对应的磁盘位图和索引位图
中的信息改为0；②删除dir 目录中dir1 的目录项内容，并且更新其对应
的位图中信息变为0；
第47 题解析：
(1) TCP 建立连接三次握手；确认序号1001。
(2) 慢启动拥塞窗口增长至3MSS=1500B；发送窗口取min
(1500B,1000B)=1000B。
(3) TCP 释放四次挥手；最后报文seq=2001，ack_seq=3002，FIN=1。
(4) 全程4 个RTT，总耗时4×5ms=20ms。
